# UserManagement
The User Management System is a web application built with React, Django REST Framework, and SQL. It allows administrators to securely add, view, update, delete, search, and manage user records. The system uses APIs for communication, ensures data accuracy, and provides efficient user administration through a centralized dashboard.
